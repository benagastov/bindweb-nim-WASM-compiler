# Terser

Vendored Terser 5.47.1 browser bundle used by `javascript-protection.mjs`.
The adjacent `LICENSE` preserves the upstream BSD license. This dependency
is tracked under `src/` so fresh clones and clean builds do not depend on
the ignored, disposable `web/vendor/` toolchain cache.

Upstream: https://github.com/terser/terser
